/**
 * NOTE: The shadcn/ui <Form /> primitives depend on `react-hook-form`, which is
 * not a project dependency. None of the app currently imports from here, so we
 * ship an empty module to keep the build clean. If you need the form helpers,
 *   1. `npm install react-hook-form`
 *   2. Restore the original shadcn/ui form primitives from
 *      https://ui.shadcn.com/docs/components/form
 */
export {};
